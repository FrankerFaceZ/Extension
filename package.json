{
  "name": "ffz-extension-manifest-v3",
  "private": true,
  "type": "module",
  "scripts": {
    "clone": "node clone.mjs",
    "build": "node build.mjs",
    "clean": "rimraf dist",
    "clean:sources": "rimraf client && rimraf addons"
  },
  "dependencies": {
    "git-pull-or-clone": "^2.0.2",
    "rimraf": "^3.0.2",
    "fs-extra": "^11.1.1"
  }
}
